#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:
exec-once = fcitx5 &
# 3-channel format: 3/4 the cache/memory of argb, and bgr needs no byte swap
# when decoding. NOTE: the format is part of the cache key, so changing it
# invalidates every existing cache entry.
exec-once = awww-daemon --format bgr
# Kill BOTH possible process names (qs and quickshell are the same binary) before
# relaunching, so a config reload always converges to exactly one instance.
exec = pkill -x quickshell; pkill -x qs; quickshell &
exec-once = cursor-clip --daemon
